using UnityEngine;
using System.Collections;

public class TowerComponentBehaviour : MonoBehaviour {

    public enum TowerComponentType { Damage = 1, Range = 2, FireRate = 3 };

    public TowerComponentType towerCompType;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
